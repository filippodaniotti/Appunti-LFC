\documentclass{standalone}
\usepackage[utf8]{inputenc}
\usepackage{amsmath}
\usepackage[linesnumbered,ruled,vlined]{algorithm2e}

\begin{document}
    \begin{algorithm}[H]
        \DontPrintSemicolon

        \SetKwData{States}{states}
        \SetKwData{Symb}{symbol}

        \SetKwFunction{Closure}{\(\varepsilon\)-closure}
        \SetKwFunction{Mark}{mark}
        \SetKwFunction{Unmark}{unmark}
        \SetKwFunction{NC}{nextChar}
        \SetKwFunction{Moved}{\(\textrm{move}_d\)}

        \(T_0\) = \Closure{\(\{s_\}\)}\;
        \(R\) = \(T_0\)\;
        \While{qualche \(T \in R\) è unmarked} {
            \Mark{\(T\)}\;
            \ForEach{\(a \in \mathcal{A}\)} {
                \(T'\) = \Closure{\(\bigcup_{t \in \textrm{\States}}\) \Moved{t, a}}
            }
            \If{\(T'\) \(\neq \emptyset\)} {
                \Moved{\(T\), a} = \(T'\)\;
                \If{\(T' \notin R\)} {
                    add \(T'\) to \(R\)\;
                    \Unmark{\(T'\)}\;
                }
            }
        }

        \ForEach{\(T \in R\)} {
            \If{\((T \cap R) \neq \emptyset\)} {
                set \(T \in E\)\;
            }
        }

        % \States = \Closure{\(\{s_0\}\)}\;
        % \Symb = \NC{}\;
        % \While{\Symb \(\neq\) \texttt{\$}} {
        %     \States = \Closure{\(\bigcup_{t \in \textrm{\States}}\) \Move{t, \Symb}}\;
        %     \Symb = \NC{}\;
        % }
        % \eIf{\States \(\cap \textrm{ } F \ne \emptyset\)}
        %     {
        %         \Return{true}\;
        %     }
        %     {  
        %         \Return{false}\;
        %     }

        \caption{subsetConstruction(NFA \(\mathcal{N}\)}
    \end{algorithm}
\end{document}